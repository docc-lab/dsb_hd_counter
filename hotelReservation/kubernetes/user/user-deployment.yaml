apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.22.0 (955b78124)
  creationTimestamp: null
  labels:
    io.kompose.service: user
  name: user
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: user
  strategy: {}
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.22.0 (955b78124)
        sidecar.istio.io/statsInclusionPrefixes: cluster.outbound,cluster_manager,listener_manager,http_mixer_filter,tcp_mixer_filter,server,cluster.xds-grp,listener,connection_manager
        sidecar.istio.io/statsInclusionRegexps: http.*
      creationTimestamp: null
      labels:
        io.kompose.service: user
    spec:
      containers:
        - command:
            - user
          image: docclabgroup/hotelreservation:panic-fixed-1.3
          name: hotel-reserv-user
          ports:
            - containerPort: 8086
          env:
            # Keep timing ON for the user service. Recent image builds
            # carry a latent bug (fixed in interceptor/options.go, commit
            # pending image rebuild) where ENABLE_TIMING unset installs a
            # timing interceptor with a nil aggregator -> SIGSEGV on the
            # first CheckUser RPC. Setting it here (not via kubectl set
            # env) survives the eval harness re-applying this manifest.
            - {name: ENABLE_TIMING, value: "true"}
          resources:
            requests:
              cpu: "1"
            limits:
              cpu: "3"
          securityContext:
            capabilities:
              add:
              - SYS_ADMIN
      restartPolicy: Always
status: {}
