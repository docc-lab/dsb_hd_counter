
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-db-pod
  labels:
    blueprint.service: user-db-pod
spec:
  replicas: 1
  selector:
    matchLabels:
      blueprint.service: user-db-pod
  template:
    metadata:
      name: user-db-pod
      labels:
        blueprint.service: user-db-pod
    spec:
      containers:
      
        - name: user-db-ctr
          image: mongo
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
      restartPolicy: Always
      hostname: user-db-pod
