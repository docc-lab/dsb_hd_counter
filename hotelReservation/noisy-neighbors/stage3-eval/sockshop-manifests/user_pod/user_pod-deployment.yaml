
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-pod
  labels:
    blueprint.service: user-pod
spec:
  replicas: 1
  selector:
    matchLabels:
      blueprint.service: user-pod
  template:
    metadata:
      name: user-pod
      labels:
        blueprint.service: user-pod
    spec:
      containers:
      
        - name: user-ctr
          image: docclabgroup/sockshop_user_ctr
          imagePullPolicy: Always
          resources:
            requests:
              cpu: "2"
              memory: "1Gi"
            limits:
              cpu: "3"
              memory: "2Gi"
          env:
            - name: USER_SERVICE_GRPC_BIND_ADDR
              value: "0.0.0.0:12345"
            - name: USER_DB_DIAL_ADDR
              value: "user-db-pod:27017"
            - name: ZIPKIN_DIAL_ADDR
              value: "zipkin-pod:9411"
          ports:
            - containerPort: 12345
      restartPolicy: Always
      hostname: user-pod
